using System;

namespace library
{
    // Making it internal means it cannot be accessed outside te assembly
    internal class RateCalculator
    {
        public int Calculate(Customer customer)
        {
            return 0;
        }
    }
}